# 결정 기록

| 결정 | 이유 | 적용 범위 / 확인 |
|---|---|---|
| 설치된 Unity 2022.3.20f1와 Blender 5.2.1 유지 | 업그레이드 없이 실행 가능한 환경 활용 | 프로토타입 |
| Built-in 렌더링, 외부 패키지 추가 없음 | 초기 화면 구성과 자동화 단순화 | 프로토타입 |
| 가로 16:9 고정 지형, 직교 카메라 | Windows 작은 창과 시설 가독성 | 구도 사용자 검토 |
| 따뜻한 일몰, 노란 벽, 회색 지붕, 초록과 흙색 | 참고 이미지의 색면·농촌 생활감 반영 | 색감 사용자 검토 |
| 토끼 주민 1종, 6명 정지 배치 | 의인화 동물 샘플, 외형 교체 가능 | 최종 종족 아님; 사용자 검토 |
| Blender 스크립트로 모델 제작, FBX 명시적 출력 | Unity의 .blend 자동 변환 의존을 피하고 원본 보존 | ArtSource는 Assets 밖에 보관 |
| --factory-startup 사용 | 기존 사용자 애드온의 백그라운드 오류 회피 | 사용자 Blender 설정은 수정하지 않음 |
| 생성 루트와 ManualEdits 분리 | 반복 생성·수동 편집 보존 | 생성 루트와 Generated 폴더 내부 편집은 재생성 시 교체됨 |
| 2단계 자동 착수 금지 | 아트와 화면 품질을 먼저 확인 | 사용자 시각적 피드백 후 결정 |
| 주민은 고정 안전 경로를 순회 | 2단계에서 살아 있는 풍경을 검증하고 자율 판단을 3단계로 분리 | 프로토타입 한정 |
| 180초 낮밤 주기 | 짧은 관찰에서 복수 시간대를 볼 수 있도록 함 | 속도는 설정값 |
| 환경음은 AudioSource 구조만, 볼륨 0 | 외부 음원 없이 시작하라는 사용자 선택 | 실제 음원은 이후 승인 또는 제공 시 추가 |
| 3단계 생활 비중은 균형형 | 낮의 일·운반, 일몰의 감상, 밤의 휴식을 함께 관찰 | 가중치와 피로 값은 `VillageAutonomySettings`에서 조정 |
| 행동 장소는 단일 소유 예약 | 같은 벤치·밭 자리에서 주민이 겹치는 것을 방지 | 완료·실패·비활성화·시간 초과 시 해제 |
| 장애물 기반 8방향 A* 및 안전한 구간 단순화 | 고정 중앙 경유점 방식의 관통·정체를 교체 | 흙길 우선, 빈 잔디 우회 허용 |
| 공통 스냅샷의 예측 회피 및 이동 전 충돌 검사 | 접촉 후 강제 밀어내기를 제거 | 몸통 반경 .45, 장애물 여유 .1 |
| F키 진단 화면은 기본 숨김 | 일반 화면의 생활감을 유지하면서 선택 근거를 점검 | Game view에서 주민별 행동·피로·예약 표시 |
| 발 애니메이션과 실제 오디오는 보류 | 사용자가 추후 일괄 교체할 계획 | 3단계 범위에 포함하지 않음 |

## 생성 및 수동 편집 계약

- 자동 관리: `Assets/Art/Generated`, 장면의 `GeneratedVillage`, 생성 스크립트에서 관리하는 렌더·플레이어 설정.
- 수동 관리: 장면의 `ManualEdits`와 그 밖의 사용자 루트, `Assets/Art/Manual` 등 Generated 밖의 자산.
- 사용자가 외형을 수정하려면 원본을 별도 .blend로 복사하고 FBX를 Generated 밖에 출력한다. 생성 Prefab 자체를 고치면 재생성 때 교체된다.
- 재생성은 기존 장면을 열고 GeneratedVillage만 교체하며 자산 경로·GUID를 유지한다. 수동 자산은 Generated 자산의 이름 변경·삭제에 의존하지 않게 관리한다.
- 저장 시스템과 영구 주민 ID는 이후 구현한다. 현재 Resident_01~06은 장면 이름이며 저장 형식으로 확정하지 않는다.

## 이동 보완 결정

- 기존 부지런함·느긋함은 유지하고 길 선호도만 고정 시드로 추가한다. 성향은 안전한 선택 사이의 선호에만 영향을 준다.
- 장애물에는 정지에 필요한 거리까지 검사해 벽 앞 급정지를 줄인다. 막힌 선호 방향은 고집하지 않고 안전한 출구를 찾아 제자리 회전한다.
- 이동 대기에는 걷기 피로를 부과하지 않고, 감상도 작업 피로를 부과하지 않는다. 운반 중 실제 이동에는 운반 피로를 적용한다.
- ActivitySlot은 Unity 직렬화 안정성을 위해 동일 이름의 별도 파일로 분리했다.
- 검사 결과는 보정 함수의 자체 거리값이 아닌 실제 위치와 이동 구간에서 측정한다. 이전 고정 경로 검사 통과 기록은 사물 관통 방지의 증거로 사용하지 않는다.

## 4단계 결정

| 결정 | 이유 | 적용 범위 / 확인 |
|---|---|---|
| 첫 자원은 당근과 식량 | 생산 흐름을 작게 검증 | 목재·화폐·확장은 5단계 이후 |
| 식량 시작/최대 12/24, 수확당 3 | 짧은 관찰에서 생산과 소비를 모두 확인 | VillageResources 기본값 |
| 부족은 완만한 생산 우선 회복 | 게임 오버 없이 생활 흐름을 유지 | 작업·운반 75%, 휴식 60% |
| 생산·균형·여유 3단계 | 주민 직접 명령 없이 마을 방향을 제시 | HUD와 VillagePriority |
| HUD는 기본 표시, F 진단과 분리 | 일상 화면을 간결하게 유지 | Tab으로 HUD 숨김 |
