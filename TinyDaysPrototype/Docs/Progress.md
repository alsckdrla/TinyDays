# 진행 기록 — 2026-09-13

## 현재 상태

**4단계 생산·방향 제시 구현·자동 검증 완료 / 사용자 시각 확인 대기.** 5단계 이후는 착수하지 않았다. 최종 단계 통과는 아직 아니다.


## 4단계 생산·방향 제시 검증

- 두 밭의 성장·수확 상자·창고 전달·식량 저장과 새벽 소비를 연결했다.
- 식량 부족 시 생산 우선, 작업·운반 효율 75%, 휴식 회복 60% 보정을 적용하고 재고 회복 시 정상으로 되돌린다.
- 생산·균형·여유 우선순위, HUD 숨김, 일시정지·1×·2×·4× 시간 제어를 추가했다.
- Unity 자동 검증에서 자원 순환, 수량 범위, 부족 보정, 시간 제어, 기존 900초 생활·이동·재생성 검사를 통과했다.
## 이번 이동 보완 검증

- 장애물 기반 A*, 곡선 추종, 속도·회전 제한, 예측 회피, 성향별 양보·길 선호와 정체 복구를 구현했다. 접촉 후 강제 위치 보정은 제거했다.
- 주민의 기본 이동 속도 배율을 1.2배로 올렸다. 주민별 기본 속도 차이와 가속·회전 제한은 유지한다.
- 정면 교행·교차·추월·정지 주민 우회·좁은 통로·가까운 시작 거리·성향 비교, 두 장애물 형태, 길 선호도 및 동적 장애물 등록 검사를 통과했다. 실제 좌표 궤적은 `NavigationScenarios.csv`에 저장했다.
- 시나리오에서 0.05초당 회전은 약 7.5도 이하, 가속·감속은 약 2.5단위/초² 이하였다. 좁은 길도 급정지 없이 완료했다.
- 같은 교행 조건에서 느긋한 주민은 약 1.95초 기다리고 부지런한 주민은 대기 없이 우회했다. 길 선호도 비교에서 흙길 사용 비율은 약 12.5%와 98.2%였다.
- 최종 900초 시뮬레이션에서 6명 각각 네 가지 행동을 완료했다. 실제 이동 구간의 주민 최소 간격은 약 0.919단위였고, 장애물 관통과 30초 이상 지속되는 정체는 없었다. 접근 실패는 주민별 0~3회였고 예약 해제 후 복귀했다.
- 강제 비활성화·건물 내부 목적지·막힌 예약 장소에서 예약 해제를 확인했다. 장면 재생성·ManualEdits 보존·12개 장소 ID 복원과 기존 카메라·낮밤·식물·연기·무음 구조 검사를 통과했다.
- 일반 Editor Play Mode 30초 관찰과 영상은 `NavigationObservation.txt`, `Captures/Navigation`에 별도 기록한다. 가속 시뮬레이션과 실시간 관찰을 구분한다.
- 실제 관찰 중 발견한 연기 축별 속도 곡선 모드 불일치 경고도 수정했다.

## 실제 구현

- 새 Unity 2022.3.20f1 프로젝트, Built-in 렌더링. 기존 설치 버전 유지, 외부 패키지 추가 없음.
- 28×21 고정 디오라마, 집 2채, 공동 창고 1채, 밭 2구역, 흙길·울타리·벤치·꽃·풀·관목·나무·돌·상자·통.
- Blender 5.2.1로 13종 모델 제작, 원본 .blend와 생성 Python 및 FBX 보관.
- 교체 가능한 토끼 Prefab 1종과 주민 6명. 모델 계층은 판단 코드와 분리했다.
- 직교 카메라: WASD/방향키, 휠, Home. 낮/일몰/밤: 1/2/3.
- 생성 자산/장면 영역과 ManualEdits 분리, 재생성 자동화 및 로컬 Git 관리.
- 주민 6명은 피로·시간대·고정 시드 개인 성향에 따라 밭 작업, 상자 운반, 벤치 휴식, 풍경 감상을 선택한다. 기존 순회 경로는 자율 판단을 사용할 수 없을 때의 안전장치로만 남긴다.
- `ActivityCoordinator`가 밭 4곳, 운반 3곳, 벤치 2곳, 감상 3곳을 고유 ID로 관리한다. 한 장소는 한 주민만 예약하며 완료·실패·비활성화·시간 초과 시 해제한다.
- `VillageAutonomySettings`에서 주민 수, 이동 속도, 피로 증감, 행동 시간, 선택 가중치와 목적지 시간 제한을 조정할 수 있다.
- `ResidentBrain`은 예약 실패·목적지 제거·접근 시간 초과 후 짧게 기다렸다가 다른 행동을 고른다. 운반 중에는 손에 작은 상자를 보이지만 자원 수치는 바꾸지 않는다.
- 긴 이동은 0.25단위 장애물 격자의 경로 탐색과 안전한 구간 단순화를 사용한다. 공통 스냅샷으로 주민 충돌을 예측하고, 이동 전 실제 구간을 검사한다.
- `F`키의 `AutonomyDiagnostics`는 기본 숨김이며, 주민별 현재 행동·선택 이유·피로·예약 장소와 목적지를 Game view에 표시한다.
- Blender 토끼는 팔·다리 메시를 보존해 절차적 걷기·대기 표현을 적용했다. 풀·꽃·작물은 각기 다른 위상으로 약하게 흔들린다.
- 굴뚝 3개에 로우폴리 입자 연기를 적용하고, 180초 연속 낮밤 및 1·2·3 비교 키를 유지했다.
- 환경음 AudioSource·볼륨 구조를 만들었으나, 사용자 선택에 따라 실제 음원과 재생은 추가하지 않았다.

## 주요 파일

- `Assets/Scenes/TinyDays.unity`: 실행 장면.
- `ArtSource/TinyDays_ArtLibrary.blend`, `Tools/generate_art.py`: 원본 아트 및 생성기.
- `Assets/Art/Generated`: FBX, 공통 재질, 교체 가능한 Prefab 13종.
- `Assets/Scripts/DioramaCamera.cs`, `LightingPresets.cs`: 런타임 조작과 표현.
- `Assets/Scripts/ResidentWanderer.cs`, `GentleSway.cs`, `ChimneySmoke.cs`, `DayNightCycle.cs`, `AmbientAudioSettings.cs`: 이동 표현과 2단계 풍경 표현.
- `Assets/Scripts/VillageAutonomySettings.cs`, `ActivityCoordinator.cs`, `ResidentBrain.cs`, `AutonomyDiagnostics.cs`: 3단계 자율 판단·예약·진단.
- `Assets/Editor/BuildDiorama.cs`, `VerifyDiorama.cs`: 장면 생성과 자동 검증.
- `Tools/Rebuild.ps1`: Blender→Unity 생성/검증 실행. `Tools/OpenUnity.ps1`: 프로젝트 열기.
- `Docs/References/TinyDays_VisualTarget.png`: 사용자 참고 이미지 원본 복사.

## 이전 단계 검증 기록

- Blender `--background --factory-startup --python-exit-code 1`: 정상 종료, 13종 FBX 및 .blend 생성.
- Unity 라이선스 확인, 프로젝트 생성, FBX 가져오기·스크립트 컴파일·장면 열기 성공.
- Unity Play Mode 진입 후 60회의 Editor update 이후 검사. 실제 런타임 Error/Exception/Assert 기록 없음.
- 장면을 연속 재생성해 생성 오브젝트 수 유지 및 ManualEdits의 시험 오브젝트와 위치 보존 확인.
- 주민 6명 확인, 주민 높이 1.7~2.1 단위·집 높이 4~4.7 단위 검사 통과. 실제 모델의 재질과 셰이더 확인.
- 카메라 메서드를 호출해 이동, X/Z 범위 제한, 최소/최대 확대, 기본 위치·배율 복귀 검사 통과.
- 3개 조명 프리셋 적용과 밤에만 켜지는 현관 조명 상태 검사 통과.
- 실제 Unity Camera.Render로 960×540, 1280×720의 낮·일몰·밤 6장 저장하고 모두 육안 확인. 모델·시설·밭이 구분되고 화면 내에 포함됨.
- Play Mode에서 300초를 빠르게 시뮬레이션해 모든 주민의 반복 이동·분산 대기·다리 움직임·안전 범위 준수 확인.
- 식물 100개 이상에 서로 다른 흔들림 위상, 굴뚝 연기 3개 재생, 무음 AudioSource, 180초 낮밤 보간과 현관등 전환을 확인.
- `Motion_08.png`, `Motion_34.png`, `Motion_60.png`으로 풍경의 시간대별 화면을 추가 캡처하고 육안 확인.
- 900초 논리 시뮬레이션에서 주민 6명의 작업·운반·휴식·감상 수행, 휴식 후 재작업, 개인별 작업 횟수 차이, 단일 소유 예약, 막힌 목적지에서 다른 행동으로 복귀를 확인했다.
- `F` 진단의 표시·숨김과 런타임 오류 없음도 자동 검사했다. 진단 글자는 IMGUI 오버레이이므로 Camera.Render 캡처가 아니라 Unity Game view에서 확인한다.
- 주민 관통 피드백 후 고정 안전 경로망과 주민 분리 조향을 추가하고 Unity 재생성·900초 자율 검증을 다시 통과했다.
- 자동 검증 결과: `Docs/Verification.txt`. 원시 실행 로그는 `Logs/`에 보관하며 로컬 Git에서는 제외.

## 발견·수정한 문제

1. 사용자 Blender 애드온들이 백그라운드 실행 시 오류 발생 → 기본 설정 실행으로 분리. 사용자 설정 변경 없음. 최종 생성 로그에는 해당 오류 없음.
2. FBX 루트 단위/축 변환을 배치가 덮어써 모델이 작아짐 → 원본 변환을 Visual 자식에 유지하고 독립 부모로 배치. 실제 월드 크기 회귀 검사 추가.
3. 지붕 면이 안쪽을 향해 지붕이 비어 보임 → 면 순서 수정, 캡처로 확인.
4. 뒤쪽 건물 입구가 반대로 보임 → 입구가 흙길을 향하도록 회전, 옆면 창 추가.
5. 검증용 오브젝트 제거가 저장되지 않음 → 장면 변경 명시 후 저장, 재검증 통과.
6. 분리 토끼의 일부 렌더러와 입자 렌더러가 재질 검사에 섞임 → 주민 전체 Bounds와 MeshRenderer만 검사하도록 조정.
7. 연기 입자가 사각형으로 보임 → 로우폴리 돌 메시를 입자로 사용하고 미리 채워 정지 화면에서도 읽히게 조정.

## 검증하지 않은 부분과 한계

- 실제 키보드·마우스를 물리적으로 입력한 검사는 하지 않았다. 기본 InputManager의 WASD·방향키 매핑 및 런타임 메서드를 확인했다.
- 6장은 Unity 카메라 렌더 캡처이며 실제 데스크톱 창 스크린샷은 아니다.
- Windows 독립 실행 파일, 프레임 속도, 장시간 안정성, 저장·불러오기는 6단계 대상이며 이번에 구현·측정하지 않았다.
- 모델은 교체 가능한 초기 샘플이다. 참고 이미지보다 지형과 길이 규칙적이고, 소품 밀도·재질·접지 음영이 단순하다. 현대 시설·큰 UI·강한 흐림은 의도적으로 제외했다.
- 밤은 주민과 시설을 구분할 수 있도록 주변광을 남겼다. 사용자 화면에서 더 밝거나 어둡게 조정할 수 있다.
- 이번 검증은 900초 가속 시뮬레이션과 별도 일반 Editor Play Mode 30초 관찰이다. 실제 15분 연속 화면 관찰과 키·마우스 물리 입력은 미검증이다.

## 사용자가 확인할 사항

1. 영상 또는 Unity Play Mode에서 주민이 미리 대각선으로 비키고, 사물을 우회하며, 모서리에서 자연스럽게 감속하는지 확인한다.
2. F키로 성향·양보 대상·재탐색 이유를 확인하고, 주민별 기다림과 우회 차이가 원하는 분위기인지 판단한다.
3. `README.md` 순서대로 키 1·2·3, 카메라 조작, 무음 기본값을 확인한다.

## 다음 작업

사용자 피드백으로 4단계를 통과한 뒤에만 5단계 작은 자율 발전을 설계한다. 권장 설정은 Astra medium + 플랜 모드다.

작업 전 할 일과 권장 모델을 알리고, 작업 후 **작업: … | 확인: … | 다음: …** 한 줄로 보고한다. 이번 작업 중 모델 설정 변경은 하지 않았다.